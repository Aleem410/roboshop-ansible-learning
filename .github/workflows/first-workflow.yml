name: My First Workflow
on:
  push:
  workflow_dispatch:

jobs:
  First Workflow:
    runs-on: ubuntu-latest

    steps:
      - name: List the files
        uses: actions/checkout@v4
        run: |
          ls -l
          pwd